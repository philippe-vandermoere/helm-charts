---
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "symfony.fullname" . }}-web
  labels:
    {{- include "symfony.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
data: {{- include "symfony.secret.env" (dict "context" .Values.web.php "global" .) | nindent 2 }}
